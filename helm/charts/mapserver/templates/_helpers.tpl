{{/*
This chart uses the 'common' library chart for shared template helpers.
All naming, labels, and resource definitions are defined in charts/common/templates/_helpers.tpl
and should be modified there rather than in individual chart templates.
*/}}